{
    "name": "rewardrally-api-playwright",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "test": "npx playwright test",
        "test:headed": "npx playwright test --headed",
        "test:debug": "npx playwright test --debug"
    },
    "devDependencies": {
        "@playwright/test": "^1.39.0",
        "@types/node": "^24.10.0",
        "dotenv": "^16.0.0"
    }
}
